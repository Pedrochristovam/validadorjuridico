# 📝 Comandos Git para Atualizar o Repositório

## 1. Navegar até a pasta do projeto:
```powershell
cd "C:\Users\User\OneDrive\Desktop\Validador Juridico"
```

## 2. Verificar status das alterações:
```powershell
git status
```

## 3. Adicionar todas as alterações:
```powershell
git add .
```

## 4. Fazer commit das alterações:
```powershell
git commit -m "Atualização: Regras de validação detalhadas e correções no backend"
```

## 5. Enviar para o repositório remoto:
```powershell
git push origin main
```

**OU se a branch for `master`:**
```powershell
git push origin master
```

---

## 📋 Resumo dos comandos em sequência:

```powershell
cd "C:\Users\User\OneDrive\Desktop\Validador Juridico"
git status
git add .
git commit -m "Atualização: Regras de validação detalhadas e correções no backend"
git push origin main
```

---

## 🔍 Verificar qual branch você está usando:
```powershell
git branch
```

## 📦 Se precisar verificar o repositório remoto:
```powershell
git remote -v
```





